# ===== ChatDataAgent 依赖清单 =====
# 环境：conda activate langchain1.2（已全部满足，无需下载）
langchain>=1.3.0
langchain-core>=1.5.0
langgraph>=1.2.0
langgraph-checkpoint-sqlite>=3.1.1
langchain-deepseek>=1.1.0
openai>=2.0.0
pandas>=2.0.0
flask>=3.0.0
flask-cors>=6.0.0
python-dotenv>=1.0.0
pymysql>=1.1.0
# 可选：表语义 embedding 预筛（不装则自动降级，不影响核心功能）
# fastembed>=0.3.0
